# ============================================================================
# Argentina AlfaBeta Scraper - Configuration Template
# ============================================================================
# INSTRUCTIONS:
# 1. Copy this file to the appropriate location:
#    - RECOMMENDED: %USERPROFILE%\Documents\ScraperPlatform\config\Argentina.env.json
#    - LEGACY: Same directory as this file and rename to .env
# 2. Replace placeholder values with your actual values
# 3. NEVER commit the actual .env file with real secrets to git!
# ============================================================================

# ============================================================================
# ALFABETA CREDENTIALS (SECRETS)
# ============================================================================
# AlfaBeta login credentials
# IMPORTANT: Keep these secret! Never commit to git!
ALFABETA_USER=your_email@example.com
ALFABETA_PASS=your_password_here

# ============================================================================
# PROXY CONFIGURATION (SECRETS)
# ============================================================================
# Format: https://username:password@host:port
# Multiple proxies can be listed, one per line
# IMPORTANT: Keep these secret! Never commit to git!
PROXY_1=https://user-your_proxy_username:your_proxy_password@proxy.example.com:10000
PROXY_2=https://user-your_proxy_username:your_proxy_password@proxy.example.com:10000
PROXY_3=https://user-your_proxy_username:your_proxy_password@proxy.example.com:10000

# ============================================================================
# SCRIPT OPTIONS
# ============================================================================
# Headless mode for browser automation
# Set to 1 for headless (no visible browser), 0 for visible browser
HEADLESS=0

# Max Rows Limit (0 = unlimited)
# This limits the number of products processed in Step 3
MAX_ROWS=0

# ============================================================================
# BROWSER RESTART / SLOWDOWN CONTROLS
# ============================================================================
# Restart Firefox after N completed products (per worker)
SELENIUM_PRODUCTS_PER_RESTART=100

# Restart browser when median product time exceeds threshold
SLOW_PAGE_RESTART_ENABLED=1
SLOW_PAGE_MEDIAN_WINDOW=15
SLOW_PAGE_MIN_SAMPLES=8
SLOW_PAGE_MEDIAN_THRESHOLD_SECONDS=60

# ============================================================================
# TOR NEW IDENTITY (OPTIONAL)
# ============================================================================
# To use NEWNYM, enable Tor's control port and set a password or cookie auth.
# Tor Browser: add ControlPort 9151 and HashedControlPassword to torrc.
# Tor service: ControlPort is usually 9051.
TOR_CONTROL_HOST=127.0.0.1
TOR_CONTROL_PORT=0
TOR_CONTROL_PASSWORD=
TOR_CONTROL_COOKIE_FILE=
TOR_NEWNYM_ENABLED=0
TOR_NEWNYM_INTERVAL_SECONDS=720

# Recommended stable setup: run standalone tor.exe (not Tor Browser UI) with:
#   SocksPort 9050, ControlPort 9051, CookieAuthentication 1
# Example cookie file path for that setup:
# TOR_CONTROL_COOKIE_FILE=C:\\TorProxy\\data\\control_auth_cookie

# ============================================================================
# 3-ROUND RETRY CONFIGURATION
# ============================================================================
# Number of Selenium retry rounds (1, 2, or 3)
# Round 1: Process all products
# Round 2: Retry products with 0 records from Round 1
# Round 3: Final retry for products still with 0 records
SELENIUM_ROUNDS=3

# Pause between rounds in seconds (default: 60)
# Allows system to stabilize between rounds
ROUND_PAUSE_SECONDS=60
