# ============================================================================
# Belarus RCETH Scraper - Configuration Template
# ============================================================================
# INSTRUCTIONS:
# 1. Copy this file to the appropriate location:
#    - RECOMMENDED: %USERPROFILE%\Documents\ScraperPlatform\config\Belarus.env.json
#    - LEGACY: Same directory as this file and rename to .env
# 2. Replace placeholder values with your actual values
# 3. NEVER commit the actual .env file with real secrets to git!
# ============================================================================

# ============================================================================
# SCRIPT OPTIONS
# ============================================================================
# Headless mode for browser automation
# Set to 1 for headless (no visible browser), 0 for visible browser
HEADLESS=1

# Tor Browser (same as Argentina) - use Firefox/Tor Browser with Tor proxy
# Set to 1 to use Tor Browser, 0 to use Chrome (default: 0 - direct connection)
# NOTE: rceth.by may not work well with Tor - use direct connection (0) if experiencing issues
SCRIPT_01_USE_TOR_BROWSER=0
# Require Tor proxy (fail if Tor not running). Set 0 to allow fallback to direct.
REQUIRE_TOR_PROXY=0
# Auto-start Tor proxy if not running (uses tor.exe from Tor Browser)
AUTO_START_TOR_PROXY=0

# Tor New Identity (request new circuit/IP) - requires control port (standalone Tor on 9051)
# Enable to request NEWNYM when driver is recycled (every N INNs)
SCRIPT_01_TOR_NEWNYM_ON_RECYCLE=1
# Control port: 9051 for standalone Tor (C:/TorProxy), 9151 for Tor Browser (if enabled)
TOR_CONTROL_HOST=127.0.0.1
TOR_CONTROL_PORT=9051
# Cookie file for auth (standalone Tor creates C:/TorProxy/data/control_auth_cookie)
# TOR_CONTROL_COOKIE_FILE=C:/TorProxy/data/control_auth_cookie

# ============================================================================
# STEP 01 OPTIONS - RCETH Data Extraction
# ============================================================================
# Generic name list CSV (in input/Belarus/)
SCRIPT_01_GENERIC_LIST_CSV=Generic Name.csv

# Output CSV filename
SCRIPT_01_OUTPUT_CSV=belarus_rceth_raw.csv

# Translate extracted data to English
SCRIPT_01_TRANSLATE_TO_EN=1

# Chrome options
SCRIPT_01_CHROME_START_MAXIMIZED=--start-maximized
SCRIPT_01_CHROME_DISABLE_AUTOMATION=--disable-blink-features=AutomationControlled
SCRIPT_01_DISABLE_IMAGES=1
SCRIPT_01_DISABLE_CSS=1

# Restart Chrome driver every N INNs to prevent memory buildup (default 15)
# Reduces ChromeDriver crashes on long runs (e.g. 50+ INNs)
SCRIPT_01_RECYCLE_DRIVER_EVERY_N=15

# Selenium HTTP read timeout in seconds (default 300). Prevents "Read timed out (read timeout=120)" when using Tor.
# SCRIPT_01_DRIVER_HTTP_TIMEOUT=300

# ============================================================================
# STEP 00 OPTIONS - Backup & Clean
# ============================================================================
# Files/directories to preserve during cleanup
SCRIPT_00_KEEP_FILES=
SCRIPT_00_KEEP_DIRS=runs,backups

# ============================================================================
# PIPELINE OPTIONS
# ============================================================================
# Log file prefix
PIPELINE_LOG_FILE_PREFIX=Belarus_run_
