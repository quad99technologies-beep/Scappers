# Platform foundation dependencies
# Install: pip install -r requirements/platform.txt

# SQLite working store (stdlib — no extra dep needed)

# DuckDB transform + QC + export layer
duckdb>=1.1.0

# OpenTelemetry tracing
opentelemetry-api>=1.22.0
opentelemetry-sdk>=1.22.0
opentelemetry-exporter-otlp-proto-grpc>=1.22.0

# OpenTelemetry metrics + logs
opentelemetry-exporter-otlp-proto-http>=1.22.0

# Scrapy (for requests-based scrapers)
Scrapy>=2.11.0

# Already in existing requirements (listed for reference):
# requests, beautifulsoup4, lxml, pandas, tenacity, python-dotenv
